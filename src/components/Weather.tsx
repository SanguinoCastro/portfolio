import Card from './weather_components/Card';
import Search from './weather_components/Search';

const Weather = () => {
  return (
    <div className=' w-full h-full border-rad flex'>
      <Search />
    </div>
  );
};
export default Weather;
